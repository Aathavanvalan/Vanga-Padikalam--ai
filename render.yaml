services:
  - type: web
    name: vangapadikalam-backend
    env: node
    region: oregon
    plan: free
    buildCommand: npm install --prefix backend
    startCommand: npm start --prefix backend
    envVars:
      - key: GEMINI_API_KEY
        sync: false
